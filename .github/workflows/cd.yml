# name: CD

# on:
#   push:
#     branches:
#       - main

# jobs:
#   build-and-push:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v3

#       - name: Login to Docker Hub
#         uses: docker/login-action@v2
#         with:
#           username: ${{ secrets.DOCKER_USERNAME }}
#           password: ${{ secrets.DOCKER_PASSWORD }}

#       - name: Build and push Docker image
#         uses: docker/build-push-action@v3
#         with:
#           context: .
#           push: true
#           tags: ${{ secrets.DOCKER_USERNAME }}/capstone-flask-app:latest

# #   deploy:
# #     runs-on: self-hosted # Use your Alibaba Cloud runner here
# #     needs: build-and-push 

# #     steps:
# #       - name: SSH into HTTP servers and deploy
# #         run: |
# #           # Replace with your actual deployment commands
# #           ssh user@http_server_1 "docker pull ${{ secrets.DOCKER_USERNAME }}/capstone-flask-app:latest && docker compose up -d"
# #           ssh user@http_server_2 "docker pull ${{ secrets.DOCKER_USERNAME }}/capstone-flask-app:latest && docker compose up -d"